tap "azure/functions"
tap "hashicorp/tap"
tap "homebrew/services"
tap "ngrok/ngrok"
tap "runpod/runpodctl"
# Run your GitHub Actions locally
brew "act"
# Official Amazon AWS command-line interface (work)
brew "awscli"
# Microsoft Azure CLI (work — Azure OpenAI etc.)
brew "azure-cli"
# Azure Functions Core Tools (work)
brew "azure/functions/azure-functions-core-tools@4"
# Container runtimes on MacOS (and Linux) with minimal setup
brew "colima"
# Load/unload environment variables based on $PWD
brew "direnv"
# Pack, ship and run any application as a lightweight container
brew "docker"
# Isolated development environments using Docker
brew "docker-compose", link: false
# OCR (Optical Character Recognition) engine — pytesseract (sandbox)
brew "tesseract"
# Play, record, convert, and stream select audio and video codecs — imageio-ffmpeg (sandbox)
brew "ffmpeg"
# Cross-platform library for audio I/O — PyAudio (sandbox)
brew "portaudio"
# GitHub command-line tool
brew "gh"
# Interpreter for PostScript and PDF — pdf tooling (sandbox)
brew "ghostscript"
# Distributed revision control system
brew "git"
# Quickly rewrite git repository history
brew "git-filter-repo"
# Git extension for versioning large files (gitconfig declares the lfs filter)
brew "git-lfs"
# Go toolchain (needed by the gopls `go` directive below)
brew "go"
# Kubernetes package manager (was link:false only because a manual
# /usr/local/bin/helm shadowed it on the Intel machine — link it on arm64)
brew "helm"
# Run lightweight k3s clusters in docker (was a manual /usr/local install)
brew "k3d"
# Tools and libraries to manipulate images in select formats
brew "imagemagick"
# Lightweight and flexible command-line JSON processor
brew "jq"
# Kubernetes command-line interface
brew "kubernetes-cli"
# Access large language models from the command-line
brew "llm"
# Run a Kubernetes cluster locally
brew "minikube"
# Simple tool to make locally trusted development certificates
brew "mkcert"
# NCurses Disk Usage
brew "ncdu"
# Manage multiple Node.js versions
brew "nvm"
# Create, run, and share large language models (LLMs)
brew "ollama", link: false
# Object-relational database system
# Minimal system Python (uv manages all project/dev Pythons)
# Simplified and community-driven man pages
brew "tldr"
# Display directories as trees (with optional color/HTML output)
brew "tree"
# Extremely fast Python package and project manager — THE Python toolchain
brew "uv"
# Internet file retriever
brew "wget"
# Terraform — show-and-destroy infra (was installed but missing from the dump)
brew "hashicorp/tap/terraform"
# runpodctl is a CLI tool to manage your GPU pods on RunPod.
brew "runpod/runpodctl/runpodctl", trusted: true
cask "font-meslo-lg-nerd-font"
# Set of tools to manage resources and applications hosted on Google Cloud
cask "gcloud-cli"
# Reverse proxy, secure introspectable tunnels to localhost
cask "ngrok"
# Open-source code editor
cask "visual-studio-code"
# Rust-based terminal
cask "warp"
# Google Antigravity — AI IDE + agent platform (self-installs CLIs to ~/.antigravity*)
cask "antigravity"
cask "antigravity-ide"
# SQL GUI (Le Wagon data-setup baseline)
cask "dbeaver-community"
# Team comms
cask "slack"

# ── VS Code / Antigravity extensions ──────────────────────────────────────────
# SINGLE SOURCE: packages/vscode-extensions.txt, installed by install.sh (step 6b)
# on macOS AND Linux. Inline `vscode` stanzas removed — they only ran on macOS and
# drifted from the real 48-extension set.
#
# ── Global npm CLIs + go tools ────────────────────────────────────────────────
# brew bundle does NOT support `npm`/`go` stanzas (they silently no-op — this was
# a real gap). They now live in install.sh (step 6c) + packages/npm-globals.txt.
